name: Manual action

# configure manual trigger
on:
  workflow_dispatch:

jobs:
  manuel_action:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Manual action
        run: |
          npm install
          npm run manualAction
